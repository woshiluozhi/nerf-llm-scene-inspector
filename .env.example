# Optional. The default planner does not require any API key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Optional paths if you keep upstream repos outside this project.
NERFSTUDIO_REPO=
LERF_REPO=
OPENNERF_REPO=
